{
  "name": "react-router-suspense-packages",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "yarn workspace react-router-suspense build",
    "test": "jest --projects ./packages/*/"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
